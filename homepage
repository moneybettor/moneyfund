<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MoneyFund - World's Biggest Hedge Fund</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', 'Helvetica Neue', 'Arial', sans-serif; /* Modern, professional font stack */
        }

        body {
            overflow-x: hidden;
            background: linear-gradient(45deg, #143227, #1a3c34, #2e4f3f, #355e46, #3a6b4f, #4a7b5e); /* 6 deep green stops */
            background-size: 600% 600%; /* Smooth, dynamic lines */
            animation: gradientShift 20s ease infinite; /* Elegant flow */
            color: #ffffff; /* Unified white text */
            position: relative;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        #particles-js, #particles-js-secondary {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        #particles-js-secondary {
            z-index: 0; /* Secondary particles behind primary */
        }

        .content {
            position: relative;
            z-index: 2;
            text-align: center;
            padding: 50px 20px; /* Spacious padding */
        }

        .hero {
            padding: 64px 24px; /* Reduced 20% from 80px 30px */
            background: linear-gradient(135deg, #1e3b2f, #2a4b3a); /* Dark green gradient for hero */
            border-radius: 12px; /* Soft, modern radius */
            border: 1px solid rgba(232, 185, 35, 0.25); /* Subtle gold border */
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.4), 0 0 10px rgba(46, 79, 63, 0.2); /* Shadow with green glow */
            max-width: 720px; /* Reduced 20% from 900px */
            margin: 0 auto;
            margin-top: 70px; /* Balanced margin */
        }

        .hero h2 {
            font-size: 2.72rem; /* Reduced 20% from 3.4rem */
            font-weight: 700; /* Bold for emphasis */
            margin-bottom: 20px;
            color: #ffffff; /* White text */
            letter-spacing: -0.03em; /* Tight spacing for luxury */
            text-transform: uppercase; /* Authority */
        }

        .hero p {
            font-size: 1.28rem; /* Reduced 20% from 1.6rem */
            font-weight: 400;
            margin-bottom: 40px;
            color: #ffffff; /* White text */
            line-height: 1.6; /* Enhanced readability */
        }

        .cta-button {
            display: inline-block;
            padding: 14.4px 32px; /* Reduced 20% from 18px 40px */
            background: linear-gradient(45deg, #2e4f3f, #4a7b5e); /* Money-green gradient */
            color: #ffffff; /* White text */
            text-decoration: none;
            font-weight: 600; /* Semi-bold */
            font-size: 0.92rem; /* Reduced 20% from 1.15rem */
            border-radius: 8px; /* Soft radius */
            border: 1px solid rgba(232, 185, 35, 0.35); /* Subtle gold border */
            transition: background 0.3s ease, transform 0.6s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.6s ease, box-shadow 0.3s ease;
            box-shadow: 0 3px 12px rgba(0, 0, 0, 0.3); /* Subtle shadow */
        }

        .cta-button:hover {
            background: linear-gradient(45deg, #3a6b4f, #5a8b6e); /* Lighter green gradient */
            transform: translateY(-3px); /* Pronounced lift */
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.4); /* Enhanced shadow */
        }

        .cta-button.slide-out {
            opacity: 0;
            transform: translateX(-20px) scale(0.8) rotate(-5deg); /* Slide left, scale down, slight rotation */
        }

        .cta-button.slide-in {
            opacity: 1;
            transform: translateX(0) scale(1) rotate(0deg); /* Slide in, scale up, no rotation */
        }

        @media (max-width: 768px) {
            .content {
                padding: 30px 15px;
            }

            .hero {
                padding: 40px 16px; /* Reduced 20% from 50px 20px */
                margin-top: 40px;
                border-radius: 10px;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            }

            .hero h2 {
                font-size: 1.92rem; /* Reduced 20% from 2.4rem */
            }

            .hero p {
                font-size: 1.12rem; /* Reduced 20% from 1.4rem */
                margin-bottom: 30px;
            }

            .cta-button {
                padding: 11.2px 22.4px; /* Reduced 20% from 14px 28px */
                font-size: 0.8rem; /* Reduced 20% from 1rem */
                border-radius: 6px;
            }
        }
    </style>
</head>
<body>
    <div id="particles-js"></div>
    <div id="particles-js-secondary"></div>
    <div class="content">
        <div class="hero">
            <h2>World's Biggest Hedge Fund</h2>
            <p>The Ticker is MONEY</p>
            <a href="https://moneyfund.com/aboutmoney" class="cta-button" id="cta-button">Read The Whitepaper</a>
        </div>
    </div>
    <!-- Include particles.js library for background effect -->
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
        // Primary particles configuration
        particlesJS("particles-js", {
            particles: {
                number: { value: 100, density: { enable: true, value_area: 800 } },
                color: { value: "#a8b5a2" }, /* Muted green */
                shape: { type: "circle" },
                opacity: { value: 0.5, random: true },
                size: { value: 3, random: true },
                move: { enable: true, speed: 1.5, direction: "none", random: false }
            },
            interactivity: {
                detect_on: "canvas",
                events: { onhover: { enable: true, mode: "repulse" }, onclick: { enable: true, mode: "push" } },
                modes: { repulse: { distance: 100, duration: 0.4 }, push: { particles_nb: 3 } }
            }
        });

        // Secondary particles configuration
        particlesJS("particles-js-secondary", {
            particles: {
                number: { value: 40, density: { enable: true, value_area: 1000 } },
                color: { value: "#d4a017" }, /* Subtle gold */
                shape: { type: "circle" },
                opacity: { value: 0.4, random: true },
                size: { value: 5, random: true },
                move: { enable: true, speed: 0.8, direction: "none", random: false }
            },
            interactivity: {
                detect_on: "canvas",
                events: { onhover: { enable: false }, onclick: { enable: false } },
                modes: {}
            }
        });

        // Button rotation logic
        const buttons = [
            { text: "Read The Whitepaper", href: "https://moneyfund.com/aboutmoney" },
            { text: "Create a Wallet", href: "/wallets" },
            { text: "Launch a Coin", href: "/deploy" },
            { text: "Launch a Fund", href: "/launcher" },
            { text: "Launch a DAO", href: "/voter" },
            { text: "Launch a Dividend Pool", href: "/stake" },
            { text: "Swap Tokens", href: "/DEX" },
            { text: "Vote", href: "/vote" },
            { text: "Claim Dividends", href: "/dividends" },
            { text: "Mass Airdrop", href: "/Airdropper" },
            { text: "Gamble", href: "/blackjack" },
            { text: "Chat", href: "/chat" }
        ];

        let currentButtonIndex = 0;
        const ctaButton = document.getElementById("cta-button");

        function rotateButton() {
            ctaButton.classList.remove("slide-in");
            ctaButton.classList.add("slide-out");

            setTimeout(() => {
                currentButtonIndex = (currentButtonIndex + 1) % buttons.length;
                ctaButton.textContent = buttons[currentButtonIndex].text;
                ctaButton.href = buttons[currentButtonIndex].href;
                ctaButton.classList.remove("slide-out");
                ctaButton.classList.add("slide-in");
            }, 600); /* Match slide-out animation duration */
        }

        // Initial slide-in
        ctaButton.classList.add("slide-in");

        // Rotate every 2.2 seconds
        setInterval(rotateButton, 2200);
    </script>
</body>
</html>
